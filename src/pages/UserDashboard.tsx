const UserDashboard = () => {
    return (
        <div>
            <h1>Dashboard</h1>
            <p>This page is only accessible to registered and authenticated users.</p>
        </div>
    );
};

export default UserDashboard;
